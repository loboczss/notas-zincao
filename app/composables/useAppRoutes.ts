import {
  AppRoute,
  PublicRoutes,
  getPageTitle,
  notaHistoricoRoute,
  notaRetiradaRoute,
} from '../constants/routes'

export const useAppRoutes = () => ({
  AppRoute,
  PublicRoutes,
  getPageTitle,
  notaRetiradaRoute,
  notaHistoricoRoute,
})
